{
 "dependencies": {
  "concurrently": "^7.0.0",
  "express-async-handler": "^1.2.0"
 },
 "scripts": {
  "start": "concurrently \"cd ./backend && pnpm run server\" \"cd ./client && pnpm run dev\"",
  "heroku-postbbuild": "NPM_CONFIG_PRODUCTION=false pnpm i client && pnpm run build client"
 }
}
